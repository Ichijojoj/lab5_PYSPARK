apiVersion: batch/v1
kind: Job
metadata:
  name: scala-data-mart-job
  namespace: bigdata
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: scala-data-mart
        image: datamart-scala-k8s:latest
        imagePullPolicy: IfNotPresent
        envFrom:
        - configMapRef:
            name: spark-k8s-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: oracle-credentials
              key: oracle-password
        resources:
          requests:
            memory: "1024Mi"
            cpu: "500m"
          limits:
            memory: "1536Mi"
            cpu: "1000m"